name: CI
on: [push, pull_request]
jobs:
  #tailwindcss-update:
  #  name: Generate Tailwind CSS
  #  runs-on: ubuntu-latest
  #  steps:
  #    - name: Checkout
  #      uses: actions/checkout@v4
  #    - name: Generate Tailwind CSS
  #      uses: ZoeyVid/tailwindcss-update@main
  #      with:
  #        input: assets/tailwind.input.css
  #        output: assets/tailwind.output.css
  #        params: "--minify"
  #    - name: Commit & Push
  #      run: |
  #        git add --force assets/tailwind.output.css
  #        git config user.name "GitHub"
  #        git config user.email "noreply@github.com"
  #        git diff-index --quiet HEAD || git commit -m "ci: tailwindcss-update"
  #        git push
  lhci:
    name: Lighthouse
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lighthouse
        uses: shopify/lighthouse-ci-action@v1
        with:
          store: ${{ secrets.SHOP_STORE_OS2 }}
          password: ${{ secrets.SHOP_PASSWORD_OS2 }}
          # Dev Dashboard app credentials. `access_token` was legacy custom-app auth;
          # Shopify stopped allowing new custom apps in January 2026.
          client_id: ${{ secrets.SHOP_CLIENT_ID }}
          client_secret: ${{ secrets.SHOP_CLIENT_SECRET }}
          product_handle: ${{ secrets.SHOP_PRODUCT_HANDLE }}
          collection_handle: ${{ secrets.SHOP_COLLECTION_HANDLE }}
          pull_theme: ${{ secrets.SHOP_PULL_THEME }}
          # 0.6 matches the >= 60 Lighthouse bar and the action's own default. It was
          # pinned at 0.8, which is tighter than run-to-run noise allows on this theme.
          lhci_min_score_performance: 0.6
          lhci_min_score_accessibility: 0.8
          # lhci_github_app_token is optional; without it the job still passes/fails and
          # prints scores to its log. Add secrets.LHCI_GITHUB_APP_TOKEN to get PR comments.
  theme-check:
    name: Theme Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Theme Check
        uses: shopify/theme-check-action@v2
        with:
          token: ${{ github.token }}